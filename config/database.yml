default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: zbs_development

test:
  <<: *default
  database: zbs_test

production:
  <<: *default
  database: zbs_production
